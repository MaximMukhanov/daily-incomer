{
  "name": "eslint-config-incomer-api",
  "version": "0.0.0",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "lint": "eslint --fix && prettier --write",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "publishConfig": {
    "access": "public"
  }
}
