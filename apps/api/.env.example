# Скопируй этот файл без `.example`, для работы с локальной базой данных
API_PORT="3001"
POSTGRES_URL="postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:5432/${POSTGRES_DATABASE}"
POSTGRES_PRISMA_URL="postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:5432/${POSTGRES_DATABASE}?pgbouncer=true&connect_timeout=15"
POSTGRES_URL_NON_POOLING="postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:5432/${POSTGRES_DATABASE}"
POSTGRES_USER="devdb"
POSTGRES_HOST="localhost"
POSTGRES_PASSWORD="devdb"
POSTGRES_DATABASE="devdb"